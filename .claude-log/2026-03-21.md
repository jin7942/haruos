# 2026-03-21 작업 로그

## 10:00 - [기술스택 확정 + 아키텍처 설계]

### 요청 내용
기술스택 결정 및 시스템 아키텍처 설계

### 수행한 작업
- ADR-001 작성: 기술스택 확정 (Vite+React 통일, 셀프호스팅 Docker Compose)
- system-architecture.md v1.3 작성 (10개 섹션)
- 애착개발패턴 11개 패턴 적용

### 결과
아키텍처 설계 완료.

## 14:00 - [모노레포 초기화]

### 요청 내용
프로젝트 전체 초기화 (모노레포, 앱 4개, 패키지 3개, infra, Docker, Flyway, CI)

### 수행한 작업

**Phase 1: 루트 설정 (9개 파일)**
- package.json, pnpm-workspace.yaml, tsconfig.json, turbo.json
- .nvmrc (Node 22), .prettierrc, .editorconfig, .npmrc
- .gitignore 보강 (pnpm, turbo, coverage, flyway)

**Phase 2: packages (12개 파일)**
- shared-types: ApiResponse<T>, PaginatedResponse<T> 인터페이스
- shared-utils: 빈 초기화
- shared-ui: 빈 초기화

**Phase 3: apps 4개 스캐폴딩 (38개 파일)**
- console-api: NestJS 11 + Swagger + TypeORM + ConfigModule
- tenant-api: 위와 동일 구조
- console-web: Vite 6 + React 18 + Tailwind + TanStack Query
- tenant-web: 위와 동일 구조 (port 5174)

**Phase 4: NestJS 공통 코드 (16개 파일)**
- BaseEntity, SoftDeletableEntity (TypeORM 공통 필드)
- Exception Hierarchy (Custom → Business/Technical, 8개 구체 예외)
- GlobalExceptionFilter (ApiResponse 형식 에러 변환)
- ApiResponseInterceptor (성공 응답 자동 래핑)
- JwtAuthGuard + @Public() 데코레이터

**Phase 5: 비즈니스 모듈 (~90개 파일)**
- console-api 7개 모듈: auth, tenant, aws, provisioner, domain, monitoring, billing
- tenant-api 15개 모듈: core/(auth,ai-gateway,storage,doc-engine,clickup), haru/(orchestrator,context,batch,watchdog), agents/(project,schedule,document,knowledge,file)
- 포트/어댑터 DI 바인딩, 엔티티 상태 전이 메서드

**Phase 6: infra (13개 파일)**
- Docker: docker-compose.yml, Dockerfile 3개, nginx.conf
- Flyway: console V1 (17 테이블), tenant V1 (22 테이블 + pgvector)
- placeholder: terraform, ansible, cloudformation, scripts

**Phase 7: CI + .env (2개 파일)**
- .env.example, .github/workflows/ci.yml

**Phase 8: 검증**
- pnpm install 성공
- packages 빌드 성공
- console-api, tenant-api 빌드 성공 (strictPropertyInitialization: false 수정)
- console-web, tenant-web 타입체크 성공

### 결과
모노레포 초기화 완료. 약 180개 파일 생성. 전체 빌드 통과.

## 04:57 - [auth 모듈 엔티티/DTO/포트 추가]

### 요청 내용
console-api auth 모듈에 엔티티 2개, DTO 4개, 포트/어댑터 각 1개 추가

### 수행한 작업
- entities/email-verification.entity.ts 생성
- entities/refresh-token.entity.ts 생성
- dto/verify-email.request.dto.ts 생성
- dto/refresh-token.request.dto.ts 생성
- dto/change-password.request.dto.ts 생성
- dto/auth.response.dto.ts 생성 (UserSummaryVo, SignupResponseDto, LoginResponseDto, TokenResponseDto)
- ports/mail-sender.port.ts 생성
- adapters/console-mail.adapter.ts 생성

### 결과
8개 파일 생성 완료.

## 05:15 - [auth 모듈 핵심 파일 구현]

### 요청 내용
console-api auth 모듈 핵심 파일 6개 구현/교체

### 수행한 작업
- `common/guards/jwt-auth.guard.ts`: JwtService 주입 + Bearer 토큰 검증 로직 추가 (TODO 제거)
- `modules/auth/auth.service.ts`: signup, login, verifyEmail, refreshAccessToken, changePassword 5개 메서드 구현
- `modules/auth/auth.controller.ts`: 5개 엔드포인트 + Swagger 데코레이터 + @Public() 적용
- `modules/auth/auth.module.ts`: EmailVerification/RefreshToken 엔티티 등록, MailSenderPort DI 바인딩
- `main.ts`: CORS, GlobalExceptionFilter, ApiResponseInterceptor 추가
- `app.module.ts`: TypeORM forRootAsync + JwtModule global 등록 + APP_GUARD(JwtAuthGuard)

### 결과
6개 파일 교체 완료. auth 모듈 핵심 로직 구현됨.

## 05:30 - [auth 모듈 테스트 + 검증]

### 요청 내용
auth 모듈 단위 테스트 작성 + 빌드/테스트 검증

### 수행한 작업
- auth.service.spec.ts 작성 (14개 테스트 케이스)
  - signup: 정상 생성, 이메일 중복
  - login: 정상 로그인, 이메일 없음, 비밀번호 틀림
  - verifyEmail: 정상 인증, 토큰 없음, 이미 인증, 만료
  - refreshAccessToken: 정상 갱신, 유효하지 않은 토큰
  - changePassword: 정상 변경, 비밀번호 틀림, 사용자 없음
- bcrypt → bcryptjs 교체 (네이티브 바인딩 문제 해결)
- pnpm install + build + test 통과 확인

### 결과
14개 테스트 통과. 빌드 성공. auth 모듈 end-to-end 구현 완료.

## 05:45 - [Flyway 시드 + AppModule 수정]

### 요청 내용
Flyway 공통코드 시드 데이터 생성 + AppModule에 CommonCodeModule 위치 조정

### 수행한 작업
- `infra/flyway/console/V2__seed_common_codes.sql` 생성 (11개 코드 그룹, 42개 코드)
  - TENANT_STATUS, PLAN_TYPE, AWS_CREDENTIAL_STATUS, PROVISIONING_STATUS
  - DOMAIN_TYPE, DOMAIN_STATUS, DNS_PROVIDER, SSL_STATUS
  - SUBSCRIPTION_STATUS, METRIC_TYPE, ALERT_TYPE
- `apps/console-api/src/app.module.ts`: CommonCodeModule을 AuthModule 바로 아래로 이동

### 결과
2개 파일 생성/수정 완료.

## 05:09 - [feat] CommonCode 모듈 구현

### 요청 내용
console-api에 공통코드(CommonCode) 모듈 구현 (9개 파일)

### 수행한 작업
- 9개 파일 생성 (entities 2, dto 4, service 1, controller 1, module 1)
- app.module.ts에 CommonCodeModule 등록
- @nestjs/cache-manager, cache-manager 패키지 설치

### 결과
TypeScript 컴파일 성공. 9개 파일 생성 완료.

## 06:00 - [feat] Tenant 모듈 구현

### 요청 내용
console-api tenant 모듈 전체 구현

### 수행한 작업
- `entities/tenant.entity.ts` 교체: create() 팩토리 메서드 추가, 컬럼 길이/기본값 보강, activate() 상태전이 CREATING->ACTIVE로 수정, TSDoc 보강
- `dto/update-tenant.request.dto.ts` 신규 생성 (name, description optional 수정용)
- `tenant.service.ts` 교체: CRUD + suspend/resume 구현. slug 중복검증, 소유권 검증(findOwnedTenant) 포함
- `tenant.controller.ts` 교체: JWT user.sub 기반 전 엔드포인트 구현, Swagger 데코레이터 보강
- `tenant.module.ts` 교체: 동일 구조 유지

### 결과
5개 파일 교체/생성 완료. TypeScript 컴파일 성공.

## 06:30 - [test] CommonCode + Tenant 단위 테스트 작성

### 요청 내용
console-api의 공통코드 모듈과 테넌트 모듈 단위 테스트 작성

### 수행한 작업
- `common-code.service.spec.ts` 생성 (11개 테스트)
  - findAllGroups: 캐시 히트/미스
  - findGroupByCode: 정상 조회, 존재하지 않는 그룹
  - createGroup: 정상 생성, 중복 예외
  - createCode: 정상 생성, 그룹 없음 예외, 코드 중복 예외
  - validateCode: 유효 코드 true, 존재하지 않는 코드 false, 그룹 없음 false
- `tenant.service.spec.ts` 생성 (14개 테스트)
  - create: 정상 생성, slug 중복 예외
  - findAll: 본인 소유 목록 반환
  - findOne: 정상 조회, 소유자 아님, 테넌트 없음
  - update: 정상 수정, 소유자 아님
  - remove: softRemove 호출 확인
  - suspend: ACTIVE->SUSPENDED 정상, CREATING 상태 예외
  - resume: SUSPENDED->ACTIVE 정상, ACTIVE 상태 예외
- auth.service.spec.ts 패턴 동일 적용 (Test.createTestingModule, jest.fn() mock)
- 상태 전이 테스트는 실제 TenantEntity 인스턴스 사용

### 결과
25개 테스트 전부 통과 (Test Suites: 2 passed).

## 07:00 - [feat] Domain 모듈 구현

### 요청 내용
console-api domain 모듈 전체 구현

### 수행한 작업
- `entities/domain.entity.ts` 보강: ERD 기반 컬럼 추가 (dnsVerifiedAt, cloudflareZoneId, cloudflareApiTokenEnc), createSubdomain()/createCustom() 팩토리, verify()/activate()/fail() 상태 전이, TSDoc
- `dto/create-domain.request.dto.ts` 신규: domain, dnsProvider, cloudflareZoneId(opt), cloudflareApiToken(opt), class-validator
- `dto/domain.response.dto.ts` 신규: static from(entity) 팩토리
- `domain.service.ts` 교체: createSubdomain, addCustomDomain, findByTenantId, remove, setPrimary, verifyDns + TenantService 주입 소유권 검증
- `domain.controller.ts` 교체: POST/GET/DELETE /tenants/:tenantId/domains, PATCH set-primary, POST verify-dns
- `domain.module.ts` 교체: TenantModule import 추가
- `domain.service.spec.ts` 신규: 18개 테스트 (CRUD + 상태전이 + 소유권 + 엔티티 상태 전이)

### 결과
18개 테스트 통과. Domain 관련 타입 에러 없음.

## 08:00 - [feat] Monitoring 모듈 구현

### 요청 내용
console-api monitoring 모듈 전체 구현

### 수행한 작업
- `ports/metric-collector.port.ts` 보강: collectEcsMetrics, collectRdsMetrics, collectS3Metrics 메서드 분리 + TSDoc
- `ports/cost-collector.port.ts` 보강: collectCosts, collectCostBreakdown 메서드 추가 + TSDoc
- `adapters/cloudwatch.adapter.ts` 교체: 3개 메서드 빈 구현
- `adapters/cost-explorer.adapter.ts` 교체: 2개 메서드 빈 구현
- `entities/metric.entity.ts` 보강: BaseEntity 제거(createdAt만 필요), precision/scale/length 지정, 복합 인덱스
- `entities/cost-record.entity.ts` 보강: date 타입 적용, precision/scale 지정, 복합 인덱스
- `entities/ai-usage-record.entity.ts` 신규: DBML 기준 전체 컬럼
- `entities/alert-config.entity.ts` 보강: lastTriggeredAt 추가, unique 복합 인덱스
- `dto/metric.response.dto.ts` 신규: static from() + Number() 변환
- `dto/cost.response.dto.ts` 신규: static from()
- `dto/ai-usage.response.dto.ts` 신규: static from()
- `dto/alert-config.response.dto.ts` 신규: static from()
- `dto/update-alert.request.dto.ts` 신규: threshold, isEnabled (class-validator)
- `monitoring.service.ts` 교체: findMetrics, findCosts, findCostBreakdown(GROUP BY), findAiUsage, findAlerts, updateAlert + TenantService 소유권 검증
- `monitoring.controller.ts` 교체: GET metrics/costs/costs-breakdown/ai-usage/alerts, PATCH alerts/:alertId
- `monitoring.module.ts` 교체: AiUsageRecordEntity + TenantModule import 추가
- `monitoring.service.spec.ts` 신규: 8개 테스트

### 결과
8개 테스트 통과. Monitoring 모듈 구현 완료.

## 09:00 - [feat] AWS 모듈 구현

### 요청 내용
console-api AWS 모듈 전체 구현 (C-AWS-001 ~ C-AWS-005)

### 수행한 작업
- `ports/aws-credential.port.ts` 보강: validateRole에 region 파라미터 추가, checkBedrockAccess에 region 추가, TSDoc 보강
- `adapters/sts.adapter.ts` 보강: Port 시그니처 맞춤, TODO 주석으로 AWS SDK 호출 위치 명시
- `entities/aws-credential.entity.ts` 보강: create() 팩토리, validate() (PENDING->VALIDATED), invalidate() (VALIDATED->INVALID) 상태 전이
- `dto/validate-aws.request.dto.ts` 신규: roleArn(ARN 정규식 검증), externalId, region
- `dto/aws-credential.response.dto.ts` 신규: static from(entity) 팩토리
- `dto/cfn-template-url.response.dto.ts` 신규: static of() 팩토리
- `aws.service.ts` 교체: getCfnTemplateUrl, validateCredential, findByTenantId, assumeRole
- `aws.controller.ts` 신규: GET /tenants/:tenantId/aws/cfn-template-url, POST /tenants/:tenantId/aws/validate, GET /tenants/:tenantId/aws/credential
- `aws.module.ts` 보강: AwsController 등록
- `aws.service.spec.ts` 신규: 8개 테스트
- `entities/aws-credential.entity.spec.ts` 신규: 5개 테스트 (상태 전이)

### 결과
13개 테스트 통과 (2 suites). AWS 모듈 구현 완료.

## 09:30 - [feat] Provisioner 모듈 구현

### 요청 내용
console-api provisioner 모듈 전체 구현 (포트/어댑터/엔티티/DTO/서비스/컨트롤러/테스트)

### 수행한 작업
- `ports/terraform.port.ts` 보강: plan(tenantId, vars), apply(tenantId, vars), destroy(tenantId) 시그니처 변경 + TSDoc
- `ports/ansible.port.ts` 보강: runPlaybook(playbook, vars) 시그니처 변경 + TSDoc
- `ports/dns.port.ts` 보강: createRecord(domain, target), deleteRecord(domain), verifyDns(domain) 추가 + TSDoc
- `adapters/terraform.adapter.ts` 교체: 새 Port 시그니처 맞춤
- `adapters/ansible.adapter.ts` 교체: 새 Port 시그니처 맞춤
- `adapters/cloudflare.adapter.ts` 교체: deleteRecord 추가
- `adapters/route53.adapter.ts` 교체: deleteRecord 추가
- `entities/provisioning-job.entity.ts` 보강: currentStep을 string으로 변경, terraformStateKey 추가, create() 팩토리, completeRollback() 추가, advanceStep() 추가
- `entities/provisioning-log.entity.ts` 신규: DBML 기준 전체 컬럼, of() 팩토리
- `entities/tenant-infra.entity.ts` 신규: DBML 기준 전체 컬럼, create() 팩토리
- `dto/provisioning-job.response.dto.ts` 신규: static from(entity)
- `dto/provisioning-log.response.dto.ts` 신규: static from(entity)
- `provisioner.service.ts` 교체: startProvisioning, getStatus, getLogs, rollback Facade + executeProvisioning/executeStep/executeRollback 내부 메서드
- `provisioner.controller.ts` 신규: POST /tenants/:tenantId/provision, GET status, GET logs, POST rollback
- `provisioner.module.ts` 교체: ProvisioningLogEntity, TenantInfraEntity, ProvisionerController 추가
- `provisioner.service.spec.ts` 신규: 15개 테스트 (Facade 7개 + 엔티티 상태전이 8개)

### 결과
15개 테스트 통과. Provisioner 모듈 구현 완료.

## 10:00 - [통합] console-api 전체 빌드/테스트 + 커밋

### 수행한 작업
- 서브에이전트 4개(aws, domain, monitoring, provisioner) 결과 통합
- console-api 전체 빌드 성공
- 전체 테스트: 4 스위트, 57개 통과
- 커밋 + 푸시 완료 (fb4a4a9)

### 결과
console-api 전체 모듈 구현 완료 (billing 제외 - P4).

## 10:30 - [docs] 다음 세션 에이전트 팀 프롬프트 작성

### 수행한 작업
- docs/next-session-prompt.md 작성
- 에이전트 팀 5명 구성: console-finisher, tenant-core, tenant-haru, tenant-agents, quality-gate
- 의존관계, 공통 규칙, 작업 범위 명시

### 결과
다음 세션에서 에이전트 팀 즉시 투입 가능.
