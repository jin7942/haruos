# 2026-06-12 작업 로그

## v0.5.0 — 의사결정 RFC→ADR 파이프라인 + 폴더 단일화

### 요청 내용
RFC→ADR 패턴 레퍼런스를 CLAUDE.md에 통합. 의사결정 폴더 단일화.
main에 다음 버전으로 통합.

### 수행한 작업
- `ADR/` → 루트 `decisions/ADR/` (git mv, ADR-001~003 보존)
- `decisions/RFC/` 신설 + README + TEMPLATE
- `decisions/README.md`(인덱스·프로세스·승격 규칙)
- CLAUDE.md "의사결정 기록 (RFC → ADR)" 절로 확장
- 문서 트리 `ADR/` 참조 갱신(README, docs/README, technical-architecture,
  system-architecture)
- 버전 0.4.0 → 0.5.0, CHANGELOG [0.5.0]

### 결과
성공. v0.5.0 main 머지 + 태그.

## v0.6.0 — RFC-001 수용 승격 (협력 기록 보존)

### 요청 내용
RFC-001(무료 사용자 격리 전략)을 수용. 문서 승격 + 원격 push + 태그.
RFC 원본은 `docs-decisions-folder` 브랜치(HoneyWater8 작성)에 존재.
해당 브랜치 커밋 기록(협력)을 살려 main에 통합. 정본은 루트 `decisions/`.

### 수행한 작업
- `docs-decisions-folder`의 RFC-001 작성 커밋(`5ce5e73`)을 cherry-pick(-x)
  → 작성자 HoneyWater8 보존
- RFC-001 본문을 `docs/decisions/RFC/` → 루트 `decisions/RFC/` 이동
  (상대경로 `../../../apps` → `../../apps` 정정)
- 브랜치의 중복 RFC README 제거(루트 버전 채택)
- RFC-001 상태 `제안` → `확정으로 승격`, 관련 ADR(ADR-004) 링크, v1.1
- ADR-004 신설: 이중 트랙(Pool+Silo) 채택. 미해결 질문은 후속 RFC로 위임
- ADR/RFC README 표 갱신, ADR TEMPLATE 흡수
- 버전 0.5.0 → 0.6.0, CHANGELOG [0.6.0]

### 결과
v0.6.0에 HoneyWater8 cherry-pick 커밋 + 김진범 승격 커밋 2개 보존.
v0.6.0 → main 머지(--no-ff) + 태그. 원격 push.

### 비고 (자기개선)
- 최초에 `docs-decisions-folder` 원격 브랜치를 못 찾고 내용을 재작성한 실수.
  → 원격 작업이 언급되면 `git fetch --all` + `git branch -a` 먼저 확인할 것.
- 협력 커밋을 살리라는 요구 = squash/재작성 금지, cherry-pick으로 작성자 보존.
